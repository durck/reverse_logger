# Copy to group_vars/edge_group_1.yml
# reverse-ssh-links.yml uses these public paths for generated client links.

rssh_ws_path: /track383211
rssh_push_path: /ping198287

# Optional: separate reverse_ssh listener per group instead of shared /ws + /push.
# main_reverse_ssh_port: 3232
