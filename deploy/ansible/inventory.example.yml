# Copy to inventory.yml (gitignored).
---
all:
  children:
    main:
      hosts:
        main1:
          ansible_host: 192.0.2.10
      vars:
        ansible_user: root
    vps_edge:
      children:
        edge_group_1:
          hosts:
            edge1:
              ansible_host: 203.0.113.20
              rssh_domain: entry1.example.com
            # edge1-b:
            #   ansible_host: 203.0.113.21
            #   rssh_domain: entry1b.example.com
        edge_group_2:
          hosts:
            # edge2-a:
            #   ansible_host: 203.0.113.30
            #   rssh_domain: entry2a.example.com
        edge_group_3:
          hosts: {}
        edge_group_4:
          hosts: {}
      vars:
        ansible_user: root
        # ansible_ssh_pass: "replace-me"
